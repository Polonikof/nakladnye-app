# VtebskieKovri — документация по проекту

Скрипт переводит накладные от поставщика «Витебские ковры» (Excel .xls)
в формат номенклатуры учётной программы. Этот файл — история решений
и точное описание логики, чтобы можно было продолжить доработку в
новом чате без повторного объяснения всего с нуля.

**Приложите этот файл вместе с `VtebskieKovri.py` в начале нового чата.**
Если есть свежая накладная, по которой результат кажется неверным —
приложите и её, и (если есть) вручную сверенный «правильный» вариант.

---

## 1. Общая идея

Поставщик присылает накладные (.xls) с 14–15 колонками (структура
описана в разделе 3). Разные **коллекции** товара (не бренды!)
кодируют информацию по-разному в столбце «Рисунок/Колорит», поэтому
для них нужны разные правила разбора. Скрипт:

1. Читает исходник.
2. Для каждой строки определяет коллекцию и категорию (ковёр/ковролин).
3. Применяет нужную схему пересчёта колонок.
4. Печатает в консоль, что нашёл (сколько строк какой коллекции).
5. Сохраняет готовый файл с именем `<имя_исходника>_VITEBSK_Обработано.xlsx`.
6. Запоминает обработанный исходник в журнале, чтобы не обрабатывать
   его повторно при следующем запуске (см. раздел 8).

Скрипт можно запускать:
- без аргументов — обработает все новые (ещё не обработанные) .xls в
  папке скрипта;
- `python VtebskieKovri.py файл.xls` — обработает конкретный файл;
- `python VtebskieKovri.py файл.xls результат.xlsx` — с явным именем
  результата;
- добавить `--force` в любом месте команды — обработать файл(ы)
  повторно, даже если они уже есть в журнале обработанных.

---

## 2. Как отличить «уже готовый» файл от исходника

Изначально ориентировались на слово «Копия»/«Исходник» в имени файла,
но это ненадёжно (пользователь называет файлы как угодно). **Правило**:
если в .xls-файле есть лист с именем `Обработанные данные` — это уже
готовый/эталонный файл, его пропускаем (функция `eto_gotovyj_fail`).

Это не единственная защита от повторной обработки — см. раздел 8 про
журнал уже обработанных исходников (это разные механизмы: один узнаёт
уже-готовый *формат* файла, другой — уже обработанный *конкретный*
исходник).

---

## 3. Структура исходника (лист «Лист 1», с заголовка)

| # | Колонка | Пример |
|---|---|---|
| 0 | № п/п | 1 |
| 1 | Продукция | "Ковер жаккардовый двухполотный, 20С3-БК/ЭФ" или "Дорожка жаккардовая двухполотная, ..." или "Покрытие ковровое прошивное..." |
| 2 | № изделия | 36465928 |
| 3 | **Рисунок/Колорит** — ключевое поле, по нему определяется коллекция | "51281/a6/РОКСОЛАНА", "p2638/a4r/124", "sh/r/34", "f1140/a2p/es" |
| 4 | Ширина, м | 0.5 |
| 5 | Длина, м | 0.5 |
| 6 | Сорт | 1 |
| 7 | Разбрак. | (пусто) |
| 8 | Кол-во | 1 |
| 9 | Ед. | "шт" / "пог.м" |
| 10 | Площадь, м2 | 0.2 |
| 11 | Цена за м2, RUB | 825.21 |
| 12 | Сумма, RUB | 165.04 |
| 13 | Штрих-код | 4814605036595 |
| 14 | (опционально) Штрих-код рулона / Код ТНВЭД — есть не у всех коллекций, скрипт его игнорирует | |

Итоговый лист «Обработанные данные» — 15 колонок:
`НомерСтроки, Тип, Форма, Наименование, ПолноеНаименвоание (так, с опечаткой —
это ожидаемое написание в вашей программе, не исправлять!),
ЕдиницаБазовая, ЕдиницаХранения, ЕдиницаХраненияКоэфф, Характеристика,
Серия, Ширина, Длина, Штрихкод, Количество, Сумма`.

> Колонка «Штрихкод» (13-я) пишется с явным числовым форматом ячейки
> `"0"` (целое число). Без этого Excel показывает 13-значный EAN13 в
> экспоненциальной записи (`4.81034E+12`) при недостаточной ширине
> колонки — сама цифра при этом не портится, но выглядит неправильно.
> Добавлено 04.09.2026 (см. историю, п.12).

---

## 4. Определение коллекции (функция `opredelit_kollekciyu`)

По строке «Рисунок/Колорит»:

| Коллекция | Признак | Пример |
|---|---|---|
| **Палитра** | начинается с `p` + цифра | `p2638/a4r/124` |
| **Шегги** | начинается с `sh/` | `sh/r/34`, `sh/p/85` |
| **Именная** (Роксолана, Консонанс и любая новая) | формат `Артикул/Цвет/КОЛЛЕКЦИЯ`, 3-й элемент — **кириллица**, заглавными | `51281/a6/РОКСОЛАНА` |
| **Неопознанная** | ничего из вышеперечисленного не подошло | → обрабатывается общим VBA-алгоритмом, коллекция в имени файла не пишется |

> ⚠️ **Важное уточнение (найдено и исправлено 04.09.2026):** раньше
> признаком «именной коллекции» было «3-й элемент — не цифра». Это
> ломалось на накладных, где 3-й элемент — просто 2–3-буквенный
> латинский код цвета/рисунка (`es`, `ct`, `ov`, `zf`, `cas` и т.п.),
> который **не является** названием коллекции. Такие коды ошибочно
> принимались за коллекцию (например «ES»), и строка уходила по
> неправильной схеме пересчёта (5.3 вместо 5.4). Исправлено: теперь
> именной коллекцией считается только то, что содержит **кириллицу**
> (реальные названия коллекций всегда пишутся кириллицей заглавными
> буквами — РОКСОЛАНА, КОНСОНАНС). Латинские буквенные коды теперь
> корректно уходят в общий VBA-алгоритм (раздел 5.4), что подтверждено
> 100%-ным совпадением с эталоном на реальном файле (см. раздел 7,
> тест-кейс «пост_вит»).

Категория (ковёр/ковролин) для «именных» коллекций определяется
**по столбцу «Продукция»** (`opredelit_kategoriya`): если в тексте есть
«ковёр»/«ковер» → ковёр, иначе (например «Дорожка…», «Покрытие
ковровое…») → ковролин. **Это НЕ по длине!** Изначально пробовали
по длине (VBA-порог 5 м), но это ломалось на «Дорожках» длиной 25 м —
подробнее в разделе 6 (история решений).

---

## 5. Схемы пересчёта по коллекциям

### 5.1. Палитра и Шегги — «очистка строки» (ближе всего к исходному VBA)
Функция `transform_kovrolin_ochistka` / `ochistit_stroku_kovrolin`.

- Тип = "Ковролин", Форма = "" (пусто).
- Наименование = строка «Рисунок/Колорит» с убранными служебными
  буквами: замены `r/`→`/`, `p/`→`/`, `//`→`/`.
  - **Только у Палитры** дополнительно убирается ведущая буква `p`
    перед цифрой артикула (баг данных поставщика): `p2638/a4/124` →
    `2638/a4/124`. У Шегги `sh` не трогаем — это законная часть кода
    коллекции, не баг.
- ПолноеНаименование = "Ковролин " + Наименование.
- Характеристика = Ширина (с запятой, без лишних .0) + " м", напр. `1,2 м`.
- Серия = "№ изделия" из исходника.
- ЕдиницаБазовая = ЕдиницаХранения = "м2", коэффициент = 1.
- **Количество = "Площадь, м2" из исходника (НЕ «Кол-во»!)** — так же
  делает и оригинальный VBA-макрос поставщика, проверено и подтверждено
  на реальных данных.

### 5.2. Именная коллекция, категория «ковёр» (Роксолана, Консонанс и т.п.)
Функция `transform_kover_imennoy`.

- Тип = "Ковер".
- Форма: `Овал`, если в «Рисунок/Колорит» есть `o/` или `/ЭФ`;
  иначе `Круг`, если Ширина = Длина; иначе `Прямоугольник`.
- Наименование = КОЛЛЕКЦИЯ + " " + Ширина*Длина + " " + Форма,
  напр. `РОКСОЛАНА 0,5*0,5 Круг`.
- ПолноеНаименование = "Ковер " + Наименование.
- Характеристика = Артикул + " " + Цвет (с пробелом!), напр. `51281 a6`.
- ЕдиницаХранения = "Ед." из исходника (обычно "шт").
- ЕдиницаХраненияКоэфф = "Площадь, м2" из исходника.
- Количество = "Кол-во" из исходника (здесь — именно Кол-во, не Площадь!).
- Серия = "" (пусто).

### 5.3. Именная коллекция, категория «ковролин» (Роксолана/Консонанс,
### например «Дорожка»)
Функция `transform_kovrolin_imennoy`. **Важно: эта схема ОТЛИЧАЕТСЯ от
5.1**, хотя обе дают Тип="Ковролин".

- Форма = "" (пусто).
- Наименование = **только название коллекции**, напр. `КОНСОНАНС`.
- ПолноеНаименование = КОЛЛЕКЦИЯ + " " + первое слово из «Продукция»,
  напр. `КОНСОНАНС Дорожка`.
- Характеристика = Артикул + " " + Цвет + " " + Ширина + " м",
  напр. `51960 a2 1 м`.
- Серия = "№ изделия" из исходника.
- Количество = "Площадь, м2" (не Кол-во).
- ЕдиницаБазовая = ЕдиницаХранения = "м2", коэффициент = 1.

### 5.4. Неопознанная коллекция — прямой перевод VBA-макроса поставщика
Функция `transform_vba_obshiy`. Категория здесь определяется **по
длине** (как в оригинальном VBA): Длина > 5 м → Ковролин, иначе Ковёр
(в отличие от именных коллекций 5.2/5.3, где категория берётся из
«Продукция» — это единственное место, где ещё используется
длина-порог). Наименование/Характеристика строятся через разбор `//`
в строке (как в оригинальном VBA-коде). Это резервная схема — как для
будущих коллекций, ещё не разобранных руками, так и (как выяснилось)
для строк с обычными буквенными кодами цвета/рисунка вместо названия
коллекции (см. врезку в разделе 4).

---

## 6. История решений (важно для будущих доработок)

1. Начинали с одной коллекции (Роксолана), схема 5.2 подтверждена на
   260 строках (0 расхождений с ручной обработкой).
2. Добавили Палитру (ковролин), обнаружили и исправили баг с лишней
   буквой `p` в начале строки — схема 5.1, подтверждена на 107 строках.
3. Пробовали классифицировать ковёр/ковролин **по длине** для всех
   коллекций (как в VBA) — это сломалось на «Дорожках» бренда
   КОНСОНАНС (длина 25 м, но структура данных как у обычного ковра).
   Из-за этого перешли на классификацию **по тексту в «Продукция»**
   для именных коллекций (Роксолана/Консонанс), а длина-порог оставили
   только как запасной вариант для неопознанных коллекций.
4. Добавили отдельную схему для ковролина именных коллекций (5.3) —
   она отличается от схемы Палитры/Шегги (5.1): наименование только
   бренд + отдельное поле для типа товара, а не «очистка строки».
   Подтверждено на данных КОНСОНАНС-Дорожка (8 строк, 0 расхождений).
5. Добавили Шегги — оказалась близкой к Палитре (та же «очистка
   строки»), но без бага с буквой в начале (это законная часть кода
   `sh`). Подтверждено на 16 строках, 0 расхождений.
6. По ходу работы термин «бренд» заменили на «коллекция» — так
   правильнее с точки зрения бизнеса (Роксолана/Консонанс/Палитра/
   Шегги — это коллекции ковровых изделий одного поставщика, не бренды).
7. Определение «исходник vs готовый файл» изначально было по слову в
   имени файла («Копия», «Исходник») — ненадёжно. Заменили на проверку
   наличия листа «Обработанные данные» внутри файла.
8. Имя итогового файла: сначала было
   `<имя_исходника>_<коллекция>_обработано.xlsx`, затем по просьбе
   пользователя заменили на `Vitebsk_<дата>_<коллекция1>_<коллекция2>...xlsx`
   (без привязки к имени исходника), а 04.09.2026 (вечером) — снова
   вернули привязку к имени исходника, теперь в виде
   `<имя_исходника>_VITEBSK_Обработано.xlsx` (см. п. 12). При коллизии
   имён — авто-суффикс ` (2)`, ` (3)` и т.д., файлы не перезаписываются
   молча.
9. **(04.09.2026) Найден и исправлен баг определения коллекции.**
   Накладная «пост_вит.xls» (54 строки, все — «Дорожка», коды цвета
   `es/ct/ov/zf/cas`) обрабатывалась неверно: буквенные коды цвета
   ошибочно принимались за названия новых коллекций. Причина —
   признак «3-й элемент не цифра» слишком широкий. Исправлено на
   «3-й элемент содержит кириллицу». После исправления — 0 расхождений
   с эталоном «пост_вит_готовый.xls» на всех 54 строках. Заодно
   добавлен пятый проверенный тест-кейс (см. раздел 7).
10. **(04.09.2026) Добавлен журнал обработанных исходников.** Раньше
    при повторном запуске .bat без аргументов скрипт находил те же
    .xls-исходники повторно (они не менялись, признака «уже готовый
    файл» на них нет) и создавал дубликаты результата. Сначала журнал
    `.vitebsk_processed.json` вёлся по хешу (md5) содержимого файла, но
    в тот же день по просьбе пользователя переделан на проверку **по
    имени файла** — так предсказуемее (переименованная копия считается
    новым файлом и обрабатывается заново). Подробности — раздел 8.
11. **(04.09.2026, ещё раз) Повторно проверили тот же баг определения
    коллекции на новых файлах пользователя** (`ФактураЕАН13_1252232`,
    `Исходник_ФактураЕАН13_1252227`, `правильноЕАН13_1252227` — те же
    данные, что и «пост_вит», просто под другим именем накладной).
    Результат уже исправленного скрипта (см. п.9) совпал с эталоном
    «правильно» на 100% (0 расхождений из 54 строк) — новых правок
    логики коллекций не потребовалось, баг был именно тот, что уже
    закрыт раньше.
12. **(04.09.2026) Формат штрихкода в результате.** В выходном .xlsx
    для колонки «Штрихкод» не был задан числовой формат ячейки —
    Excel показывал большие числа (13 цифр EAN13) в экспоненциальной
    записи (`4.81034E+12`) при недостаточной ширине колонки, хотя
    само значение хранилось верно. Эталон поставщика
    (`ФактураЕАН13_1252232...xls`) использует формат ячейки `0`
    (целое число, никакой экспоненты). Добавили `number_format = "0"`
    для колонки «Штрихкод» при записи каждой строки — теперь совпадает
    с эталоном.

---

## 7. Проверенные тестовые случаи (эталоны, 0 расхождений)

| Коллекция / случай | Категория | Строк | Ключевая фишка |
|---|---|---|---|
| РОКСОЛАНА | Ковёр | 260 | базовая схема 5.2 |
| ПАЛИТРА | Ковролин | 107 | баг с лишней "p" исправлен |
| КОНСОНАНС | Ковролин ("Дорожка") | 8 | категория по «Продукция», не по длине |
| ШЕГГИ | Ковролин | 16 | "sh" не убирается (не баг) |
| **пост_вит** (буквенные коды es/ct/ov/zf/cas + Шегги) | Ковролин | 54 | коды цвета **не** путаются с названием коллекции (см. п.9 истории) |
| **ФактураЕАН13_1252227** (те же данные, что «пост_вит», под другим именем) | Ковролин | 54 | подтверждает п.9/11 + формат штрихкода `0` |

Если в новом чате будете дорабатывать скрипт — **обязательно
перепроверяйте эти 6 эталонов на регрессию**, у вас должны сохраниться
файлы:
- `Копия_Фактура_1252186.xls` / `Копия Фактура_1252185 (1) (1).xls` (Роксолана)
- `Копия_Палитра_Фактура_0777961__1_.xls` / `Копия Фактура_0777961 (1).xls` (Палитра)
- `КОНСОНАНС_Ковролин_готовая_таблица.xls` / `Исходник КОНСОНАНС ковролин.xls` (Консонанс-ковролин)
- `Шегги_готовый_фаил_Копия_Фактура_0777896__1_.xls` / `Фактура_0777896 (1).xls` (Шегги)
- `пост_вит.xls` + `пост_вит_готовый.xls` (буквенные коды цвета)
- `Исходник_ФактураЕАН13_1252227_от_31_08_26__1_.xls` + `правильноЕАН13_1252227_от_31_08_26__1_.xls`
  (те же данные, что «пост_вит»; заодно `ФактураЕАН13_1252232_от_31_08_26.xls` —
  эталон **формата ячейки** штрихкода — колонка «Штрих-код» там задана
  с числовым форматом `0`, без него Excel показывает экспоненту)

Метод проверки: открыть оба файла (результат скрипта и эталон),
построчно сравнить все 15 колонок листа «Обработанные данные»
(с учётом того, что числа-с-плавающей-точкой вида `X.0` надо сравнивать
как целые, а `None`/`''` считать эквивалентными).

---

## 8. Журнал обработанных файлов (защита от повторной обработки)

Добавлено 04.09.2026, уточнено в тот же день (проверка сделана по
**имени файла**, а не по содержимому — так предсказуемее и понятнее
пользователю). Файл `.vitebsk_processed.json` создаётся рядом со
скриптом при первом запуске и хранит для каждого обработанного
исходника:

```json
{
  "пост_вит.xls": {
    "processed_at": "2026-09-04 06:17:53",
    "out_file": "пост_вит_VITEBSK_Обработано.xlsx",
    "rows": 54
  }
}
```

Ключ — **имя файла** (`os.path.basename`, без пути). Если один и тот
же .xls положить в папку ещё раз под тем же именем — он будет
пропущен. Если файл переименовать (например, поставщик или Windows
добавили ` (2)` к имени) — он будет считаться **новым** файлом и
обработается заново, даже если содержимое совпадает. Это осознанный
компромисс: проверка по имени проще и понятнее, чем по хешу
содержимого, но не защищает от дублей под разными именами — если это
станет проблемой, можно будет вернуться к проверке по содержимому.

Поведение:
- **Без аргументов** (обычный запуск через .bat): скрипт молча
  пропускает файлы, хеш которых уже есть в журнале, и печатает, когда
  и во что они были обработаны раньше. Обрабатывает только новые.
- **С указанием конкретного файла**: если файл уже в журнале — скрипт
  ничего не создаёт, а печатает дату/имя предыдущего результата и
  подсказку про `--force`.
- **`--force`** (можно добавить в любом месте команды) — обработать
  повторно, даже если файл уже есть в журнале. Полезно, если
  поставщик прислал исправленную версию под тем же именем, но вы
  хотите точно пересчитать заново, или если нужно перегенерировать
  результат.

Если нужно сбросить журнал целиком (например, переносите скрипт на
другой ПК или хотите начать учёт заново) — просто удалите файл
`.vitebsk_processed.json`. Ничего в самих накладных при этом не
изменится, скрипт просто "забудет", что их уже обрабатывал.

---

## 9. Известные открытые вопросы / что может понадобиться доработать

- **Форма "Овал"** (признаки `o/` и `/ЭФ` в «Рисунок/Колорит») ни разу
  не встречалась в реальных данных — логика перенесена из VBA, но не
  проверена на реальном примере. Если попадётся такая накладная —
  сверить вручную.
- **Неопознанная коллекция без буквенного кода и без кириллицы**
  (просто пустой 3-й элемент или другая структура строки) тоже ни разу
  не встречалась целиком отдельно от уже разобранных случаев — при
  появлении новой структуры «Рисунок/Колорит» стоит сверить результат
  с ручной обработкой и, возможно, завести для неё отдельную схему.
- Поставщик прислал фразу «Коллекций больше чем я указал» — значит,
  стоит ожидать что рано или поздно придёт файл новой, ещё не виденной
  коллекции или нового формата кода. Скрипт для этого спроектирован
  быть расширяемым: чтобы добавить новую коллекцию — достаточно (1)
  научить `opredelit_kollekciyu` её узнавать, (2) если у неё своя схема
  пересчёта — добавить новую `transform_...`-функцию и подключить её в
  `transform_row`.
- Если понадобится обрабатывать файлы из **разных** папок (не только
  рядом со скриптом) под одним и тем же журналом — сейчас журнал
  привязан к папке, где лежит скрипт (`STATE_FILENAME` в той же
  папке). Можно доработать, если станет нужно.

---

## 10. Расположение файлов на ПК пользователя

Рабочая папка: `C:\Накладные`. Там лежат:
- `VtebskieKovri.py` — сам скрипт (актуальная версия, с журналом
  обработанных файлов и исправленным определением коллекции).
- `Обработать_накладные.bat` — запуск двойным щелчком (без аргументов,
  обрабатывает все новые, ещё не обработанные .xls в папке).
- `.vitebsk_processed.json` — журнал обработанных файлов (создаётся
  автоматически, трогать не нужно).
- `Инструкция_для_сотрудника.docx` — инструкция по установке Python и
  запуску, отправлена сотруднику пользователя.

Python и библиотеки (`xlrd`, `openpyxl`) уже установлены и
подтверждены рабочими на этом ПК.
