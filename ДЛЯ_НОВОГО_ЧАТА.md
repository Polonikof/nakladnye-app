# Для нового чата — как продолжить этот проект

Положите этот файл (и при необходимости `VtebskieKovri_ДОКУМЕНТАЦИЯ.md`) в начало нового чата.

**Не собирайте программу с нуля.** Это уже готовое оконное приложение на Python + Tkinter, сборка в один `Накладные.exe`.

## Что это

Программа переводит накладные поставщиков в формат для учётной программы.

| Тип | Вход | Результат |
|---|---|---|
| Витебские ковры | `.xls` фактура | `*_VITEBSK_Обработано.xlsx` |
| Обои (УПД ВВП) | `.xlsx` УПД | `*_ОБОИ_Обработано.xlsx` |
| Юсуф (ковры, Турция) | packing list `.xlsx` | `*_ЮСУФ_Обработано.xlsx` |

Рабочая папка на ПК: `C:\Накладные`  
Папка обучения новым форматам: `C:\Накладные\НовыеНакладные`

Текущая версия окна: **1.7**. После «Обучить» появляется один файл `Накладные_1.7.1.exe` со справочниками внутри — его отправляют коллеге.

## Как пользуется Дмитрий

1. Обычные накладные кладёт в `C:\Накладные` → кнопка **Обработать файлы**.
   Сюда же кладётся packing list Юсуф (`Юсуф исходник №1643.xlsx`) — программа сама узнаёт формат.
2. Чтобы научить программу новой накладной, кладёт в `C:\Накладные\НовыеНакладные` **два файла**:
   - как пришло (в имени слово `исходник`)
   - как сделал руками (в имени слово `готовый`)
   → кнопка **Обучить**.
   Для Юсуфа: `Юсуф исходник №1643.xlsx` + `Готовый новый Юсуф №1643.xlsx`
   (в имени готового достаточно слова «готовый»; два пробела не мешают).
3. Агент:
   - Обои — дописывает артикулы в `oboi_catalog.json`
   - Юсуф — сохраняет `yusuf_profile.json` (формат таблицы или packing list, формы D/O)
   - поднимает версию и кладёт один `Накладные_1.7.1.exe` (правила вшиты в файл).

Подробно для человека: `КАК_РАБОТАТЬ.txt`.

## Юсуф — что должно получиться

Не формат Витебска (15 колонок «НомерСтроки / Тип / Форма»).

С версии 1.7 эталон — **отдельная таблица** (лист «Готовый»), как
`samples/Готовый новый Юсуф №1643.xlsx`:

| наименование | ед изм | полное наименование | характеристика | EAN Code | Width | Length | m2 / Unit | Quantity | Quantity / m2 | Size & Shope |
|---|---|---|---|---|---|---|---|---|---|---|
| ETNO 2*3 Прямоугольник | шт | Ковер ETNO 2*3 Прямоугольник | 9624A BURGUNDY/BURGUNDY | … | 200 | 300 | 6 | 6 | 36 | 200 |

Колонка «Size & Shope» в этом эталоне — ширина в см (не строка `200 x 300 D`).
Площади считаются числами: `Width × Length / 10000` и `× Quantity`.

Старый эталон (`samples/Готовый Юсуф №1643.xlsx`) — копия packing list с доп. колонками 12–18.
Если обучить по нему, программа снова пишет тот формат (`output_kind: packing`).

Образцы: `samples/Юсуф исходник №1643.xlsx` и `samples/Готовый новый Юсуф №1643.xlsx`.

## Главные файлы

| Файл | Зачем |
|---|---|
| `nakladnye_app.py` | Окно (кнопки, журнал). Версия берётся из `version.txt` или `BUILT_IN_VERSION` |
| `convert_nakladnaya.py` | Обработка Витебск / обои / Юсуф |
| `learn_novye.py` | Обучение по папке НовыеНакладные |
| `oboi_catalog.json` | Справочник артикул → код УТ + короткое имя |
| `yusuf_profile.json` | Формат packing list Юсуфа |
| `nakladnye.spec` | Сборка PyInstaller |
| `release/Накладные.exe` | Готовая Windows-сборка |
| `VtebskieKovri_ДОКУМЕНТАЦИЯ.md` | Правила разбора ковров, не ломать |
| `samples/` | Эталон УПД обоев и Юсуф №1643 |

## Сборка exe

На Windows: `build_exe.bat`  
На GitHub: workflow `.github/workflows/build-windows-exe.yml` (по тегу `v*`).

Готовый файл также на Releases репозитория.

## Проверка обоев

```bash
pip install -r requirements.txt
python convert_nakladnaya.py --force \
  "samples/ВВП УПД 04.09.26 № 00УТ-011419 source.xlsx" \
  samples/out_oboi.xlsx
python compare_oboi.py samples/out_oboi.xlsx "samples/ВВП УПД 04.09.26 готовый.xlsx"
```

Ожидается: `OK: 0 расхождений`.

## Проверка Юсуфа

```bash
python convert_nakladnaya.py --force \
  "samples/Юсуф исходник №1643.xlsx" \
  samples/out_yusuf.xlsx
python compare_yusuf.py samples/out_yusuf.xlsx "samples/Готовый новый Юсуф №1643.xlsx"
python -m unittest test_yusuf.py
```

Ожидается: `OK: 0 расхождений` и все тесты зелёные.

## Чего не делать

- Не переписывать программу на Next.js / сайт.
- Не менять опечатку `ПолноеНаименвоание` в выходном файле ковров — так нужно учётке.
- Не требовать Python на рабочем ПК: пользователь запускает один `.exe`.
- Не выдавать Юсуф в шапку Витебска (НомерСтроки / Тип / Форма).
